
#include <string.h>

#include "buffer.h"

static char *head = NULL;
static char *tail = NULL;

static char buffer[128][128];

char *msg_buffer() {
    return NULL;
} // msg_buffer()

char *next_msg() {
    if (head == tail) {
    }
} // next_msg()

// buffer.c
